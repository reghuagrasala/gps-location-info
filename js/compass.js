let active=false,heading=null,listeners=new Set();export function onHeading(f){listeners.add(f);if(Number.isFinite(heading))f(heading);return()=>listeners.delete(f)}export async function enableCompass(){if(!("DeviceOrientationEvent"in window))return false;try{if(typeof DeviceOrientationEvent.requestPermission==="function"){const p=await DeviceOrientationEvent.requestPermission();if(p!=="granted")return false}window.addEventListener("deviceorientation",e=>{let h=Number.isFinite(e.webkitCompassHeading)?e.webkitCompassHeading:Number.isFinite(e.alpha)?(360-e.alpha)%360:null;if(Number.isFinite(h)){heading=h;listeners.forEach(f=>f(h))}},true);active=true;return true}catch{return false}}export function isActive(){return active}